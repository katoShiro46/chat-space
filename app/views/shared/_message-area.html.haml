-@messages.each do |message|
  .message-area__name
    = message.user.name
  .message-area__time
    = message.created_at.strftime("%Y/%m/%d %H:%M")
  .message-area__comment
    - if message.comment.present?
      = message.comment
  = image_tag message.image.url, class: 'lower-message__image' if message.image.present?


